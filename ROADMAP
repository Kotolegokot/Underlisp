pattern matching
FFI
IMPORTANT: fix error rows and columns
completion in REPL
parallelism and concurrency
defconstant
condition system
full-featured module system
freeze, thaw
Lisp -> Eval
add type Read = Except Fail
recursion in let
add sugar for lambdas in let, remove flet
add macrolet
write lots of tests
when stable merge to master

1. split built-in functions and special operators adequately
2. special operators aren't functions and so aren't first-class objects and objects at all
2. read function
3. reader-macros

data types:
  fractional
  long numbers
  complex numbers
  maps
    and so forth...

standard library:
  input-output operators (format in particular, or rust's fmt)
  sort
