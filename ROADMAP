pattern matching
FFI
call stack
adequate REPL
partial exporting in modules

data types:
  fractional
  long numbers
  maps
  vectors
  arrays
    and so forth...

standard library:
  input-output operators (format in particular)
