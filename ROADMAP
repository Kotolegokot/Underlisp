pattern matching
FFI
call stack
adequate REPL
partial exporting in modules
fix error rows and columns
exceptions
add undef (unset) function
case takes pairs of the form (expr body...)
--no-prelude argument
&body
&optional

1. special operators aren't functions and so aren't first-class objects and objects at all

2. read function
3. reader-macros

data types:
  fractional
  long numbers
  maps
  vectors
  arrays
    and so forth...

standard library:
  input-output operators (format in particular, or rust's fmt)
  sort