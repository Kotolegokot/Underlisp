pattern matching
FFI
call stack
define and set should be distinct things

data types:
  fractional
  long numbers
  maps
  vectors
  arrays
    and so forth...

standard library:
  input-output operators (format in particular)
