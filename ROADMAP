pattern matching
FFI
call stack
adequate REPL
partial exporting in modules
ENV should contain LEnv, not Map String SExpr
make strings back a distinct class

data types:
  fractional
  long numbers
  maps
  vectors
  arrays
    and so forth...

standard library:
  input-output operators (format in particular)
  sort