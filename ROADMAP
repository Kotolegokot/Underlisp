pattern matching
FFI
partial exporting in modules
IMPORTANT: fix error rows and columns
completion in REPL
exceptions
parallelism and concurrency
&body
&optional
forbid rebinding special operators

1. split built-in functions and special operators adequately
2. special operators aren't functions and so aren't first-class objects and objects at all
2. read function
3. reader-macros

data types:
  fractional
  long numbers
  maps
    and so forth...

standard library:
  input-output operators (format in particular, or rust's fmt)
  sort
