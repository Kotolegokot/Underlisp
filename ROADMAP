pattern matching
FFI
call stack
adequate REPL
set previous value as it in REPL
partial exporting in modules
ENV should contain Env, not Map String SExpr
fix error rows and columns
exceptions

data types:
  fractional
  long numbers
  maps
  vectors
  arrays
    and so forth...

standard library:
  input-output operators (format in particular)
  sort